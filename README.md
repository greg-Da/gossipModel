# Gossip

Display differents gossips

To test it launch :

`bundle install`

`rails db:create`

`rails db:migrate`

`rails db:seed`

`rails s`

you can also access the elements of the database with `rails c`
